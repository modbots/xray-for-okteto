services:
  okvtwo: 
    container_name: webhookrelay
    image: webhookrelay/webhookrelayd:latest
    network_mode: host 
    command:
      - --mode 
      - tunnel
    restart: always
    environment:
      # Authentication
      - RELAY_KEY=70e90dd3-bea9-4ca8-9632-858ca734f275
      - RELAY_SECRET=o3SlW6W3Iwlr
      # One or more tunnels must be set in the .env file
      - TUNNELS=www
      - REGION=us-west # eu, au, us-west (defaults to eu)    
    
    shm_size: 2.6gb
    ports:
      - "80:80"
